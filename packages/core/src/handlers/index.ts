// Handler registry — handler registration, lookup, and invocation contracts

/**
 * Registry of event handlers.
 * Consumers register handlers; core resolves and invokes them.
 * No framework or provider logic lives here.
 */
export interface HandlerRegistry {
  // placeholder — registration and lookup API defined in future stories
}
