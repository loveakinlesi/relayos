// Runtime-internal types — core-specific contracts not suitable for @relayos/types

// Re-export shared contracts from @relayos/types for use within core
export type { BaseEntity } from '@relayos/types';
