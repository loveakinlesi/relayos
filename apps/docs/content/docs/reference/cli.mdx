---
title: '@relayos/cli'
description: CLI for RelayOS migrations, development, and execution inspection.
---

`relayos` bundles this package's `relay` bin automatically — most projects
never need to install `@relayos/cli` directly. See [CLI](/docs/cli) for the
zero-install `npx relayos@latest init` flow. Install it standalone only if
you want the CLI without the rest of `relayos` (e.g. a CI image that only
runs `relay migrate`).

## Install

<Tabs items={['pnpm', 'npm', 'yarn', 'bun']}>
  <Tab value="pnpm">
  ```sh
  pnpm add -D @relayos/cli
  ```
  </Tab>
  <Tab value="npm">
  ```sh
  npm install -D @relayos/cli
  ```
  </Tab>
  <Tab value="yarn">
  ```sh
  yarn add -D @relayos/cli
  ```
  </Tab>
  <Tab value="bun">
  ```sh
  bun add -d @relayos/cli
  ```
  </Tab>
</Tabs>

Or install globally:

<Tabs items={['pnpm', 'npm', 'yarn', 'bun']}>
  <Tab value="pnpm">
  ```sh
  pnpm add -g @relayos/cli
  ```
  </Tab>
  <Tab value="npm">
  ```sh
  npm install -g @relayos/cli
  ```
  </Tab>
  <Tab value="yarn">
  ```sh
  yarn global add @relayos/cli
  ```
  </Tab>
  <Tab value="bun">
  ```sh
  bun add -g @relayos/cli
  ```
  </Tab>
</Tabs>

```sh
relay --help
```

## Commands

| Command                                                                    | Description                                                        |
| --------------------------------------------------------------------------- | ------------------------------------------------------------------- |
| [`init [--force] [--dir <path>]`](/docs/cli/init)                          | Scaffold `relay.ts` + `relay.handlers.ts` and install packages     |
| [`migrate [--dir <path>]`](/docs/cli/migrate)                              | Apply pending schema migrations                                    |
| [`dev [--dir <path>]`](/docs/cli/dev)                                      | Run the app's dev server and tail executions live                  |
| [`trigger <provider> <eventType> [--data] [--forward]`](/docs/cli/trigger) | Simulate a signed provider webhook                                 |
| [`inspect <id> [--json] [--history] [--dir]`](/docs/cli/inspect)           | Show an execution's status, steps, and logs                        |
| [`replay <id> [--forward] [--print] [--dir]`](/docs/cli/replay)           | Replay a historical execution as a new one                         |
| [`events list [--json] [--limit] [--dir]`](/docs/cli/events)              | List recent executions, newest first                                |

Every command except `init` and `trigger` loads `relay.ts` from `--dir` (defaults to cwd) — see the full page for each command for options, examples, and output.

## Environment variables

| Variable                    | Read by                    | Purpose                                                                |
| ---------------------------- | --------------------------- | ------------------------------------------------------------------------ |
| `RELAYOS_BASE_URL`           | `trigger`, `replay`         | Base URL for webhook/replay requests when `--forward` isn't passed. Defaults to `http://localhost:3000` |
| `<PROVIDER>_WEBHOOK_SECRET`  | `trigger`                   | Signs the simulated webhook (e.g. `STRIPE_WEBHOOK_SECRET`)              |

There's no `DATABASE_URL` here — `migrate`, `dev`, `inspect`, `replay`, and `events list` all load `relay.ts` and use whatever database client it configures.

## Status icons

Executions and steps print as `✔` completed, `✖` failed, or `…` pending — used by `dev`, `inspect`, and `events list`.

## See also

- [CLI overview](/docs/cli)
- [Installation](/docs/installation)
- [Database](/docs/database)
- [Writing plugins](/docs/plugins/writing-plugins)
